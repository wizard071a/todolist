{% extends 'base.html.twig' %}

{% block body %}
    <h2 class="page-header">Add ToDo</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create ToDo" class="btn btn-primary" style="margin: 0 0 0 15px" />
    {{ form_end(form) }}
{% endblock %}